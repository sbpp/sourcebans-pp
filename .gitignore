# Windows image file caches
Thumbs.db
ehthumbs.db

# Folder config file
Desktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msm
*.msp

# =========================
# Operating System Files
# =========================

# OSX
# =========================

.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon


# Thumbnails
._*

# Files that might appear on external disk
.Spotlight-V100
.Trashes

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Netbeans
/nbproject/

# SourceBans++
/web/config.php
/web/cache/*.php
/web/cache/*.tmp
/web/cache/sessions/sess_*
*.smx

# Uploaded demo evidence (production) + synthetic demo files written by
# `./sbpp.sh db-seed` (dev). `.gitkeep` is the structural marker so the
# directory exists for fresh tarball installs and tests that probe
# `is_dir(SB_DEMOS)`. Both upload paths land MD5-hash basenames here;
# checking them in would (a) blow up the working tree on every db-seed
# and (b) leak real ban evidence into the source tree on a self-hoster
# install.
/web/demos/*
!/web/demos/.gitkeep

# Local stash file used by the install-screenshot capture flow
# (`docs/README.md` → "Capturing screenshots"). Gitignored so an
# interrupted run never leaks the panel's DB credentials into a
# commit.
/.sbpp-config-stash.php

# Local Docker dev stack
/.env
/dump-*.sql
/docker-compose.override.yml

# VSCode
.vscode

# Composer vendor dir
/web/includes/vendor

# Plugin compilation
build/
release/
plugins/
.sourceknight
.venv

# Generated by .github/workflows/release.yml; runtime falls back to `git describe`
/web/configs/version.json

# Astro + Starlight build artifacts (docs/ — see #1333). The auto-captured
# screenshots under docs/src/assets/auto/ ARE committed (the screenshot
# diff lands in the same PR as the UI change), so they're NOT listed here.
docs/node_modules/
docs/dist/
docs/.astro/
