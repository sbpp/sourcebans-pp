# These are supported funding model platforms.
#
# We point GitHub's "Sponsor this project" UI at the canonical
# /sponsor/ docs page rather than a single platform-specific account.
# That landing page lists every funding channel (GitHub Sponsors
# today, Open Collective + others once they're approved) and the
# current sponsor roll. Adding a new funding platform later is a
# one-line edit on docs/src/data/sponsors.json — this file does not
# need to change again.
#
# See issue #1416 + docs/src/content/docs/sponsor.mdx.

custom: ['https://sbpp.github.io/sponsor/']
